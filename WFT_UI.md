---
title: WFT_UI — 전자칠판 플로팅 툴 UI/UX & Design Reference
version: 1.0
related: SKILL.md (기능 명세 F-01~F-10, A-01~A-15 참조)
audience: UI 디자이너, WPF 개발자, QA
---

# WFT_UI — 전자칠판 플로팅 툴 UI/UX & Design 참조 문서

이 문서는 `SKILL.md`의 기능 명세를 **화면과 컴포넌트 수준**으로 구체화한 디자인 참조 문서다.
개발자는 이 문서의 토큰(색·크기·간격)을 WPF `ResourceDictionary`에 1:1로 매핑해 사용하고,
디자이너는 이 문서의 규칙을 벗어나는 시안을 만들지 않는 것을 원칙으로 한다.

---

## 0. 문서 사용 규칙
- 모든 수치는 **DIP(Device Independent Pixel, 96 DPI 기준)** 로 표기한다. 150%/200% 배율은 WPF가 자동 처리한다.
- 색상은 토큰 이름으로만 참조한다. (`#2D7FF9` 대신 `Color.Accent`)
- 문서에 없는 컴포넌트가 필요하면 §4 컴포넌트 규격에 먼저 추가한 뒤 구현한다.
- "MUST"는 필수, "SHOULD"는 권장, "MAY"는 선택이다.

---

## 1. 디자인 원칙

| # | 원칙 | 의미 | 판단 기준 |
|---|---|---|---|
| P1 | **원터치** | 어떤 핵심 기능도 탭 2번을 넘지 않는다 | 버튼 → 결과, 또는 버튼 → 목록 → 결과 |
| P2 | **비침입(Non-intrusive)** | 수업 자료를 가리지 않는다 | 기본 폭 64px 이하, 투명도 조절, 접기 가능 |
| P3 | **터치 우선** | 손가락 조작을 기준으로 설계한다 | 터치 타깃 최소 44px, 권장 56px, 간격 ≥ 6px |
| P4 | **교실 가독성** | 3~7m 거리·조명 반사 환경 | 다크 테마 기본, 명도 대비 ≥ 4.5:1, 라벨 병기 |
| P5 | **예측 가능** | 같은 위치에 같은 버튼 | 사용자가 순서를 바꾸기 전까지 버튼 위치 고정, 컨텍스트 버튼은 별도 구역에만 표시 |
| P6 | **조용함** | 소리·팝업·알림으로 수업을 끊지 않는다 | 오류는 토스트 1개, 소리는 타이머 종료 외 사용 금지 |

---

## 2. 디자인 토큰

### 2.1 색상 — 다크 테마(기본)

| 토큰 | 값 | 용도 | 대비(전경/배경) |
|---|---|---|---|
| `Color.Bg.Bar` | `#1C1D22` | 플로팅 바 배경 | – |
| `Color.Bg.Panel` | `#23252B` | 팝업 패널 배경 | – |
| `Color.Bg.Elevated` | `#2B2E36` | 카드, 목록 아이템 | – |
| `Color.Bg.Hover` | `#343842` | 버튼 hover | – |
| `Color.Bg.Pressed` | `#3E4350` | 버튼 pressed | – |
| `Color.Border` | `#3A3E48` | 1px 구분선 | – |
| `Color.Fg.Primary` | `#F2F3F5` | 기본 텍스트/아이콘 | 15.4:1 on Bar |
| `Color.Fg.Secondary` | `#A9AEB8` | 보조 텍스트, 라벨 | 7.6:1 on Bar |
| `Color.Fg.Disabled` | `#5E636E` | 비활성 아이콘/텍스트 | 3.1:1 (비활성 예외) |
| `Color.Accent` | `#4C8DFF` | 활성(토글 ON), 선택된 항목, 슬라이더 | 5.9:1 on Bar |
| `Color.Accent.Fg` | `#FFFFFF` | Accent 배경 위 텍스트 | 4.6:1 on Accent |
| `Color.Success` | `#3CC77E` | 저장 완료, 타이머 여유 | 8.9:1 on Bar |
| `Color.Warning` | `#F5B437` | 타이머 1분 이하, 주의 | 10.2:1 on Bar |
| `Color.Danger` | `#F0555F` | 타이머 종료, 오류, 길게 누르기 확인 | 5.5:1 on Bar |
| `Color.HitTest` | `#01000000` | 투명 영역 히트테스트 유지용 | – |

### 2.2 색상 — 라이트 테마

| 토큰 | 값 |
|---|---|
| `Color.Bg.Bar` | `#F6F7F9` |
| `Color.Bg.Panel` | `#FFFFFF` |
| `Color.Bg.Elevated` | `#EEF0F4` |
| `Color.Bg.Hover` | `#E3E6EC` |
| `Color.Bg.Pressed` | `#D6DAE2` |
| `Color.Border` | `#D1D5DB` |
| `Color.Fg.Primary` | `#15171C` |
| `Color.Fg.Secondary` | `#5B616E` |
| `Color.Fg.Disabled` | `#A5AAB5` |
| `Color.Accent` | `#2563EB` |
| `Color.Success` / `Warning` / `Danger` | `#1E9E5A` / `#C47F00` / `#D93843` |

### 2.3 색상 — 고대비 테마
- Windows 고대비 모드 감지 시(`SystemParameters.HighContrast`) 자동 적용한다.
- 배경 `#000000`, 전경 `#FFFFFF`, Accent `#FFFF00`, 테두리 `#FFFFFF` 2px. 투명도 기능은 **강제 100%**.

### 2.4 판서 팔레트 (A-01 스크린 펜)

| 이름 | 값 | 비고 |
|---|---|---|
| 빨강 | `#FF3B30` | 기본 펜 색 |
| 파랑 | `#2F7CF6` | |
| 검정 | `#111111` | 밝은 자료 위 |
| 흰색 | `#FFFFFF` | 어두운 자료 위 |
| 초록 | `#22B14C` | |
| 노랑(형광) | `#FFE94D` @ 45% | 형광펜 전용 |
| 주황(형광) | `#FF9F1C` @ 45% | 형광펜 전용 |

### 2.5 타이포그래피
- 글꼴: **Pretendard** (번들) → 폴백 `Malgun Gothic`, `Segoe UI`.
- 숫자는 `font-variant-numeric: tabular-nums` 상당(`Typography.NumeralAlignment="Tabular"`)을 적용해 타이머 자릿수 흔들림을 막는다.

| 토큰 | 크기/굵기/행간 | 용도 |
|---|---|---|
| `Type.Label` | 12 / Medium / 16 | 버튼 아래 한글 라벨 |
| `Type.Body` | 14 / Regular / 20 | 목록 항목, 설정 설명 |
| `Type.BodyStrong` | 14 / SemiBold / 20 | 목록 제목 |
| `Type.Title` | 18 / SemiBold / 24 | 패널 제목, 설정 섹션 |
| `Type.Display` | 28 / SemiBold / 34 | 온보딩 제목 |
| `Type.TimerCompact` | 40 / SemiBold / 44 | 바 내부 타이머 |
| `Type.TimerLarge` | 120 / Bold / 128 | 확대 타이머(교실 뒤 가독) |

### 2.6 간격·크기·반경

| 토큰 | 값 | 용도 |
|---|---|---|
| `Space.1` ~ `Space.6` | 4 / 8 / 12 / 16 / 24 / 32 | 일반 간격 |
| `Size.Button.S/M/L` | 44 / **56** / 72 | 버튼 한 변(정사각) |
| `Size.Icon.S/M/L` | 20 / **24** / 32 | 버튼 크기에 대응 |
| `Size.Grip` | 56 × 20 (가로 바: 20 × 56) | 드래그 손잡이 |
| `Size.Bar.Width` | Button + 8 (M 기준 64) | 세로 바 폭 |
| `Size.Panel.MinWidth` | 320 | 팝업 패널 |
| `Size.Panel.MaxWidth` | 480 | |
| `Size.Panel.MaxHeight` | 화면 높이 × 0.7 | 초과 시 스크롤 |
| `Size.Thumb` | 192 × 108 (16:9) | 창 썸네일 |
| `Radius.S/M/L` | 6 / 10 / 14 | 버튼 / 패널·카드 / 바 외곽 |
| `Border.Width` | 1 | 모든 구분선 |

> 그림자(DropShadow)는 **사용하지 않는다.** 투명도 조절 시 잔상이 생기고 렌더링 비용이 크다. 구분은 1px `Color.Border`와 배경 명도 차이로 한다.

### 2.7 투명도
| 토큰 | 값 |
|---|---|
| `Opacity.Min` | 0.20 |
| `Opacity.Default` | 0.85 |
| `Opacity.Hover` | 1.00 (옵션 ON일 때 포인터/터치 진입 시) |
| `Opacity.Panel` | 항상 1.00 (팝업 패널은 투명도 영향을 받지 않는다) |
| `Opacity.Overlay.Blackout` | 1.00 |
| `Opacity.Overlay.Spotlight` | 0.75 |

### 2.8 모션
| 토큰 | 지속 | 이징 | 용도 |
|---|---|---|---|
| `Motion.Instant` | 0 ms | – | 버튼 pressed 피드백(즉시) |
| `Motion.Fast` | 120 ms | `CubicEase Out` | hover, 토글, 슬라이더 |
| `Motion.Normal` | 180 ms | `CubicEase Out` | 패널 열기/닫기(페이드+8px 슬라이드) |
| `Motion.Slow` | 240 ms | `CubicEase InOut` | 바 접기/펼치기, 도킹 |
- Windows "애니메이션 효과 끄기" 설정 감지 시 모두 `Motion.Instant`로 대체한다.
- 바운스, 스프링, 지속 애니메이션(펄스 등)은 금지한다. 예외: 타이머 종료 시 화면 테두리 깜빡임 3회.

### 2.9 WPF 리소스 키 매핑 예시
```xml
<!-- Themes/Dark.xaml -->
<Color x:Key="Color.Bg.Bar">#1C1D22</Color>
<SolidColorBrush x:Key="Brush.Bg.Bar" Color="{StaticResource Color.Bg.Bar}"/>
<sys:Double x:Key="Size.Button.M">56</sys:Double>
<CornerRadius x:Key="Radius.S">6</CornerRadius>
<Duration x:Key="Motion.Fast">0:0:0.12</Duration>
```
테마 전환은 `Application.Resources.MergedDictionaries` 교체로 처리하고, 모든 컨트롤은 `DynamicResource`로 바인딩한다.

---

## 3. 레이아웃

### 3.1 플로팅 바 해부도 (세로, 기본)

```
┌──────┐  ← Radius.L
│ ═══  │  Grip (드래그 손잡이) 56×20, 길게 누르면 투명도 슬라이더
│  ‹   │  접기 버튼 56×28
├──────┤  1px Border
│ [▣]  │  창 전환        ┐
│ 창   │                 │
│ [⧉]  │  탭 전환        │  사용자 선택 기능 (F-08)
│ 탭   │                 │  순서 = settings.enabledActions
│ [⊞]  │  작업 보기      │
│ [◧]  │  스냅           │
│ [✎]  │  판서           │
│ [■]  │  가리기         ┘
├──────┤  1px Border (컨텍스트 구역, 조건부 표시)
│ [◁]  │  PPT 이전       ┐ 활성 창이 PowerPoint일 때만
│ [▷]  │  PPT 다음       ┘
├──────┤
│ [⚙]  │  설정 (항상 마지막, 숨김 불가)
└──────┘
폭 64 · 버튼 간격 6 · 상하 패딩 8
```

- 구역 순서 MUST: **Grip → 접기 → 사용자 기능 → 컨텍스트 기능 → 설정**.
- 컨텍스트 구역은 비어 있으면 구분선까지 함께 사라진다(빈 공간 금지).
- 버튼이 화면 높이의 80%를 넘으면 `더 보기(⋯)` 버튼으로 오버플로 패널에 수용한다.

### 3.2 가로 바
세로 바를 90° 회전한 구조로, Grip은 좌측 20×56. 라벨은 아이콘 아래에 그대로 표시한다(높이 76).

### 3.3 접힌 상태
- 원형 56px 버튼 1개(앱 아이콘)만 남는다. 탭 → 펼치기, 드래그 → 이동.
- 접힌 위치는 펼쳐진 바의 Grip 위치를 기준으로 계산해 **펼쳐도 화면 밖으로 나가지 않도록** 보정한다.

### 3.4 자동 숨김(도킹, A-13)
- 바를 모니터 가장자리 12px 이내로 드래그하면 도킹 후보 표시(가장자리 4px Accent 라인).
- 손을 떼면 바가 가장자리로 밀려 들어가고 **8px 손잡이 + 아이콘 힌트**만 남는다.
- 손잡이 탭 또는 포인터 hover 400ms → 펼침. 3초 미조작 시 다시 숨김.

### 3.5 위치 규칙
- 초기 위치: 주 모니터(또는 저장된 모니터) 우측 가장자리에서 16px, 세로 중앙.
- 드래그 중 화면 밖으로 나갈 수 없다(작업 영역 기준 클램프). 작업표시줄 위로는 올라가지 않는다.
- 모니터 구성 변경으로 저장 좌표가 유효하지 않으면 초기 위치로 리셋하고 토스트로 알린다.

### 3.6 팝업 패널 배치
- 바의 **반대편(화면 중심 방향)** 으로 열린다. 바가 우측에 있으면 좌측으로.
- 패널 세로 위치는 호출 버튼과 정렬하되 화면을 벗어나면 위/아래로 밀어 넣는다.
- 바와 패널 사이 간격 8px. 패널은 `Opacity 1.0`, 바의 투명도와 무관하다.

---

## 4. 컴포넌트 규격

### 4.1 액션 버튼 (ActionButton)
| 속성 | 값 |
|---|---|
| 크기 | `Size.Button.M` 56×56 (설정 44/72) |
| 구성 | 아이콘 24 + 라벨 12 (라벨 숨김 시 아이콘 28로 확대) |
| 반경 | `Radius.S` |
| 상태 | Default / Hover / Pressed / Active(토글 ON) / Disabled / Loading |

| 상태 | 배경 | 아이콘·라벨 | 비고 |
|---|---|---|---|
| Default | 투명 | `Fg.Primary` / `Fg.Secondary` | |
| Hover | `Bg.Hover` | 동일 | 마우스 전용, 터치는 생략 |
| Pressed | `Bg.Pressed` | 동일 | **터치 다운 즉시** 적용(0ms) |
| Active | `Accent` 15% 틴트 + 좌측 3px Accent 바 | `Accent` | 판서 ON, 가리기 ON 등 토글 |
| Disabled | 투명 | `Fg.Disabled` | 탭 버튼인데 브라우저가 아닐 때 등. 탭하면 툴팁 "브라우저 창을 먼저 선택하세요" |
| Loading | 투명 | 아이콘 자리에 16px 스피너 | 탭 목록 UIA 조회 중(최대 600ms) |

- 길게 누르기(500ms)는 **보조 메뉴**에만 사용한다(예: 스냅 버튼 길게 → 4방향 팔레트). 위험 동작 확인 용도로만 `Danger` 색을 쓴다.
- 포커스 링(키보드 접근 시): 2px `Accent`, 오프셋 2px.

### 4.2 Grip(드래그 손잡이)
- 가운데 3줄 점선(각 2×16, 간격 3) `Fg.Secondary`. 드래그 중 `Fg.Primary`.
- 길게 누르기 500ms → 투명도 슬라이더 팝업(4.5).
- 더블 탭 → 접기/펼치기 (설정으로 끌 수 있음).

### 4.3 팝업 패널 (Popover)
```
┌─────────────────────────────┐
│ 제목                 [닫기]│  Title 18, 높이 48
├─────────────────────────────┤
│ 내용 (스크롤 영역)          │
│                             │
└─────────────────────────────┘
```
- 배경 `Bg.Panel`, 테두리 1px `Border`, 반경 `Radius.M`, 패딩 12.
- 바깥 탭 / ESC / 항목 선택 / 3초(설정) 미조작 시 닫힘. 닫힘 카운트다운은 상단 1px 진행선으로 표시한다.
- 스크롤은 터치 드래그(팬) 지원, 스크롤바는 6px 오버레이형.

### 4.4 창 목록 아이템 (WindowListItem, F-01)
```
┌────────────────────────────────────┐
│ ┌──────────┐  ⬚ 프로세스 아이콘 16 │
│ │ 썸네일   │  창 제목 최대 2줄     │  높이 124
│ │ 192×108  │  (BodyStrong)         │
│ └──────────┘  프로세스명 (Body, Secondary)
└────────────────────────────────────┘
```
- 2열 그리드(패널 폭 480일 때), 1열(320일 때). 항목 간격 8.
- 현재 활성 창은 좌측 3px `Accent` 바 + 썸네일 테두리 `Accent`.
- 최소화된 창은 썸네일 대신 프로세스 아이콘 48 + "최소화됨" 배지.
- 정렬: 최근 사용 순(Z-order). 자기 자신·툴윈도우·클로크 창 제외.
- 빈 상태: "전환할 창이 없습니다" (Body, Secondary), 아이콘 없음.

### 4.5 탭 목록 아이템 (TabListItem, F-03)
- 높이 48, 좌측 파비콘 16(없으면 브라우저 아이콘), 제목 1줄 말줄임, 우측에 현재 탭이면 `●` Accent.
- 상단에 브라우저 식별 헤더: "Edge · 창 제목" / "Chrome · 창 제목". 브라우저 창이 여러 개면 헤더별 그룹.
- 30개 초과 시 상단 검색 필드 표시(터치 키보드 자동 호출하지 않음, 탭하면 호출).
- UIA 실패 상태: "탭 목록을 읽을 수 없습니다 · 이전/다음 탭 버튼을 사용하세요" + [이전 탭] [다음 탭] 버튼 2개를 패널 안에 제공한다.

### 4.6 스냅 레이아웃 피커 (SnapLayoutPicker, F-06/F-07)
```
┌───────────────────────────────────┐
│ 스냅 레이아웃                     │
│ ┌────┬────┐ ┌──┬────┐ ┌───┬───┬───┐│
│ │    │    │ │  │    │ │   │   │   ││  각 레이아웃 96×54
│ └────┴────┘ └──┴────┘ └───┴───┴───┘│
│ ┌────┬────┐ ┌─────────┐            │
│ ├────┼────┤ │  전체   │            │
│ └────┴────┘ └─────────┘            │
└───────────────────────────────────┘
```
- 레이아웃 탭 → 슬롯이 확대되어 표시되고, 슬롯 탭 → 창 목록(4.4 축소형) → 창 선택 → 배치. 다음 슬롯 자동 진행.
- 슬롯 채워지면 해당 슬롯에 창 썸네일 축소 표시. "완료" 또는 바깥 탭으로 종료.
- 빠른 스냅(좌/우/상/하)은 피커 없이 즉시 실행되는 별도 버튼으로 둔다(P1).

### 4.7 투명도 슬라이더 (OpacitySlider, F-09)
- 가로 240 × 높이 44, 트랙 4px, 썸(thumb) 28px(터치용). 좌측 `20%`, 우측 `100%`, 썸 위 현재값 툴팁.
- 드래그 중 바의 투명도가 실시간 반영된다. 손을 떼면 저장.
- 하단 토글: "터치하면 선명하게(Opacity.Hover)".

### 4.8 토글 스위치 / 체크박스
- 토글 44×24, 썸 20, ON 배경 `Accent`. 라벨은 항상 좌측, 토글은 우측 정렬.
- 기능 선택 목록(F-08)은 체크박스 대신 **토글 + 드래그 핸들(⋮⋮)** 을 사용한다.

### 4.9 토스트 (Toast)
- 바 근처(반대편) 320×56, 배경 `Bg.Elevated`, 아이콘 20 + 텍스트 1줄. 2.5초 후 자동 소멸.
- 동시에 1개만. 새 메시지가 오면 교체. 사운드 없음.
- 사용 예: "설정을 저장했습니다", "이 창은 관리자 권한으로 실행 중이어서 제어할 수 없습니다".

### 4.10 타이머 위젯 (TimerWidget, A-04)
- 바 내부 컴팩트: 폭 64, 숫자 `Type.TimerCompact`(MM:SS), 하단 진행 바 3px.
- 탭 → 확대 모드: 화면 우상단 360×200 카드, `Type.TimerLarge`. 드래그 가능. 다시 탭 → 컴팩트.
- 색상: 잔여 > 20% `Fg.Primary`, ≤ 20% `Warning`, 종료 `Danger` + 테두리 깜빡임 3회 + 짧은 알림음(설정으로 끔).
- 프리셋 버튼 1/3/5/10분 + 직접 입력(휠 피커, 터치 키보드 미사용).

### 4.11 판서 툴바 (InkToolbar, A-01)
```
[펜] [형광펜] [지우개] │ ● ● ● ● ● ● ● │ [굵기 ▾] [실행취소] [전체지우기] │ [저장] [종료]
```
- 판서 시작 시 플로팅 바 자리에서 **가로 툴바로 변형**(Motion.Slow). 위치는 바가 있던 자리 유지.
- 색 원형 32px, 선택 시 2px 흰 테두리. 굵기 3단계(2/5/10px).
- "종료"는 항상 마지막, `Danger` 아닌 `Fg.Primary`. 전체지우기는 길게 누르기 500ms.
- 툴바 자체도 Grip으로 이동 가능.

### 4.12 오버레이 (Blackout / Spotlight, A-02/A-03)
- Blackout: 전체 `#000000`, 중앙 하단에 `Fg.Secondary` 12px 안내 "화면을 탭하면 해제됩니다"(3초 후 사라짐).
- Spotlight: `#000000 @ 75%`, 원형 구멍 기본 지름 360, 두 손가락 핀치로 240~720 조절, 드래그로 이동. 우상단 [닫기].
- 오버레이가 켜진 동안 플로팅 바는 오버레이 위에 남고 해당 토글 버튼은 Active 상태.

### 4.13 설정 창 (SettingsWindow)
- 일반 활성 창(포커스 허용), 크기 760×560, 최소 640×480, 좌측 탭 내비게이션 200px.
- 탭: **기능** / **모양** / **동작** / **프로필** / **바로가기** / **정보**.
- 변경은 즉시 반영·즉시 저장(저장 버튼 없음). 하단에 "기본값으로 되돌리기" 텍스트 버튼.

---

## 5. 아이콘

### 5.1 스타일
- 24px 그리드, 1.75px 스트로크, 라운드 캡, 단색. Fluent System Icons(Regular) 스타일을 기준으로 하되 부족한 것은 동일 규격으로 제작한다.
- 형식: XAML `PathGeometry`(리소스 키 `Icon.<ActionId>`). PNG 사용 금지(DPI 대응).

### 5.2 아이콘·라벨 매핑

| ActionId | 라벨(한글, 최대 4자) | 아이콘 설명 |
|---|---|---|
| `switchWindow` | 창 전환 | 겹친 사각형 2개 |
| `prevWindow` / `nextWindow` | 이전 창 / 다음 창 | 사각형 + 좌/우 화살표 |
| `tabList` | 탭 목록 | 브라우저 탭 모양 |
| `prevTab` / `nextTab` | 이전 탭 / 다음 탭 | 탭 + 좌/우 화살표 |
| `taskView` | 작업 보기 | 3열 사각형 |
| `snapLeft` / `snapRight` / `snapTop` / `snapBottom` | 좌 / 우 / 상 / 하 스냅 | 반 채워진 사각형 |
| `snapLayout` | 레이아웃 | 4분할 그리드 |
| `pen` | 판서 | 펜 |
| `blackout` | 가리기 | 사각형 + 대각선 |
| `spotlight` | 강조 | 원 + 광선 |
| `magnifier` | 확대 | 돋보기 + `+` |
| `timer` | 타이머 | 스톱워치 |
| `pptPrev` / `pptNext` / `pptStart` / `pptEnd` | 이전 / 다음 / 쇼 시작 / 쇼 종료 | 슬라이드 + 화살표 / ▶ / ■ |
| `capture` | 캡처 | 카메라 |
| `launcher` | 바로가기 | 로켓 또는 격자 |
| `keyboard` | 키보드 | 키보드 |
| `volumeUp` / `volumeDown` / `mute` | 크게 / 작게 / 무음 | 스피커 ± / 스피커 X |
| `showDesktop` | 바탕화면 | 모니터 + 아래 화살표 |
| `randomPick` | 뽑기 | 주사위 |
| `settings` | 설정 | 톱니 |
| `collapse` / `expand` | (라벨 없음) | ‹ / › (바 방향에 따라 회전) |
| `more` | 더 보기 | ⋯ |

- 라벨은 명사형 2~4자로 통일한다. 동사형("전환하기") 금지.

---

## 6. 인터랙션 패턴

### 6.1 입력 규칙
| 입력 | 동작 |
|---|---|
| 탭 | 기본 실행 |
| 길게 누르기 500ms | 보조 메뉴/확인(진동 없음, 배경이 `Bg.Pressed`→`Accent` 15%로 채워지는 원형 진행 표시) |
| 드래그(Grip) | 이동. 이동 임계값 6px 미만은 탭으로 처리 |
| 더블 탭(Grip) | 접기/펼치기 |
| 바깥 탭 | 열린 패널 닫기 |
| 두 손가락 핀치(스포트라이트) | 크기 조절 |

### 6.2 피드백 타이밍
- 터치 다운 → Pressed 시각 피드백: **0 ms**(P3의 핵심).
- 명령 실행 → 결과 반영: 100 ms 이내가 목표. 600 ms를 넘으면 Loading 상태 표시.
- 실패 시: 버튼 2px `Danger` 테두리 300 ms + 토스트 1회. 모달 다이얼로그 금지.

### 6.3 포커스 정책 (SKILL.md §4.1 연동)
- 플로팅 바와 팝업 패널은 `WS_EX_NOACTIVATE`. 사용자가 무엇을 눌러도 아래 창의 캐럿이 유지된다.
- 설정 창, 온보딩, 타이머 확대 카드는 일반 창(포커스 허용)이다. 단, 닫히면 직전 활성 창으로 포커스를 되돌린다.

### 6.4 컨텍스트 반응
| 활성 창 | 표시되는 컨텍스트 버튼 |
|---|---|
| PowerPoint / 한쇼 | pptPrev, pptNext, pptStart(편집 모드) 또는 pptEnd(쇼 모드) |
| Chrome / Edge | tabList, prevTab, nextTab 활성화(다른 앱에서는 Disabled) |
| 동영상 플레이어 / 브라우저 전체화면 | volumeUp/Down/mute 활성 우선 |
- 컨텍스트 전환 시 버튼은 페이드(Motion.Fast)로 교체하고, 사용자 기능 구역의 위치는 절대 밀리지 않는다(P5).

---

## 7. 화면별 명세

### 7.1 첫 실행 온보딩 (3단계, 60초 이내)
1. "끌어서 옮기세요" — Grip 하이라이트, 실제 드래그 완료 시 다음.
2. "접어서 숨기세요" — 접기 버튼 하이라이트, 접었다 펴면 다음.
3. "필요한 기능만 고르세요" — 설정>기능 탭 미리보기, [바로 시작] / [기능 고르기].
- 각 단계 카드 360×200, `Type.Display` 제목, 건너뛰기 텍스트 버튼 우상단. 다시 보지 않기 자동 저장.

### 7.2 설정 > 기능 탭
```
┌ 기능 ────────────────────────────────────────────┐
│ 프로필: [기본 ▾]                                  │
│                                                  │
│ ⋮⋮  [▣] 창 전환                          ( ON) │
│ ⋮⋮  [⧉] 탭 목록                          ( ON) │
│ ⋮⋮  [⊞] 작업 보기                        (OFF) │
│ ...                                              │
│ ──────── 수업 도구 ────────                       │
│ ⋮⋮  [✎] 판서                              ( ON) │
│ ...                                              │
│ 미리보기 ▶ 우측에 실제 바 렌더링(실시간)          │
└──────────────────────────────────────────────────┘
```
- 좌측 목록(드래그 정렬), 우측 320px에 실시간 미리보기 바. 그룹: "창 관리" / "브라우저" / "수업 도구" / "시스템".
- ON 개수 0이면 설정 버튼만 남는 바를 미리보기로 보여주고 경고 문구 없이 허용한다.

### 7.3 설정 > 모양 탭
- 테마(다크/라이트/시스템/고대비), 버튼 크기(44/56/72 라디오 + 미리보기), 라벨 표시 토글, 바 방향(세로/가로), 투명도 슬라이더, 터치 시 선명하게 토글.

### 7.4 설정 > 동작 탭
- 자동 숨김, 패널 자동 닫힘(끄기/2/3/5초), 시작 시 자동 실행, 표시/숨김 전역 단축키, 스냅 레이아웃 방식(Windows 기본/자체 패널), 타이머 알림음.

### 7.5 설정 > 프로필 탭
- 프로필 카드 목록(이름, 기능 수, 사용 중 배지). 추가/복제/이름 변경/삭제(삭제는 길게 누르기). 프로필 전환 단축 버튼을 바에 추가하는 토글.

### 7.6 설정 > 바로가기 탭
- 항목: 아이콘, 이름, 대상(경로/URL), 드래그 정렬. 파일/폴더는 탐색기 드래그&드롭으로 추가 가능.

### 7.7 설정 > 정보 탭
- 버전, 로그 폴더 열기, 설정 파일 열기, 설정 초기화, 라이선스.

---

## 8. 접근성

| 항목 | 기준 |
|---|---|
| 텍스트 대비 | 본문 ≥ 4.5:1, 대형 텍스트(≥ 18px) ≥ 3:1 (§2.1 표 참조) |
| 터치 타깃 | ≥ 44×44, 인접 타깃 간격 ≥ 6 |
| 라벨 | 모든 버튼에 `AutomationProperties.Name` 한글 지정(라벨 숨김 상태에서도) |
| 키보드 | 설정 창은 Tab 순회 완전 지원. 바 자체는 마우스/터치 전용이지만 전역 단축키(표시/숨김)를 제공 |
| 고대비 | 시스템 고대비 감지 시 §2.3 자동 적용, 투명도 100% 강제 |
| 모션 | 시스템 애니메이션 끄기 존중 |
| DPI | 100/125/150/175/200% 레이아웃 검증. 비트맵 자산 금지 |
| 색 의존 | 상태를 색으로만 표현하지 않는다(Active 상태는 좌측 바 + 색, 타이머는 색 + 깜빡임) |

---

## 9. 마이크로카피 가이드

### 9.1 톤
- 존댓말 "~하세요/~합니다", 짧고 직접적. 느낌표·이모지 금지.
- 기술 용어 대신 교사가 쓰는 말: "포커스" → "선택한 창", "UIA" → 언급 금지, "프로세스" → "프로그램".

### 9.2 용어 통일표
| 사용 | 사용 금지 |
|---|---|
| 창 전환 | 윈도우 스위칭, Alt+Tab |
| 탭 목록 | 브라우저 탭 리스트 |
| 작업 보기 | 태스크 뷰 |
| 스냅 | 창 붙이기, 도킹 |
| 판서 | 잉크, 드로잉 |
| 가리기 | 블랙아웃, 커튼 |
| 강조 | 스포트라이트 |
| 투명도 | 불투명도(값은 "투명도 85%"처럼 표시하되 내부 값은 Opacity) |

### 9.3 메시지 예시
| 상황 | 문구 |
|---|---|
| 탭 버튼 Disabled | 브라우저 창을 먼저 선택하세요 |
| UIA 실패 | 탭 목록을 읽을 수 없습니다. 이전/다음 탭 버튼을 사용하세요 |
| 관리자 권한 창 | 이 창은 관리자 권한으로 실행 중이어서 제어할 수 없습니다 |
| 위치 리셋 | 모니터 구성이 바뀌어 위치를 초기화했습니다 |
| 설정 손상 | 설정 파일에 문제가 있어 기본 설정으로 시작했습니다 |
| 타이머 종료 | 시간이 끝났습니다 |

---

## 10. 상태·예외 화면

| 상황 | 표현 |
|---|---|
| 창 목록 비어 있음 | 패널 중앙 Body/Secondary 문구, 높이 120 |
| 탭 조회 중 | 아이템 자리 스켈레톤 3개(단색 `Bg.Elevated`, 애니메이션 없음) 최대 600ms |
| 기능 0개 선택 | 바에 Grip + 설정만 표시 |
| 오프스크린 복구 | 초기 위치로 이동 + 토스트 |
| 크래시 재시작 | 사용자에게 다이얼로그 없이 조용히 재시작, 다음 설정 열람 시 정보 탭에 "최근 오류 로그" 배지 |

---

## 11. 자산·명명 규칙
- 리소스 키: `Color.*`, `Brush.*`, `Size.*`, `Radius.*`, `Motion.*`, `Icon.<ActionId>`, `Type.*`(Style).
- 컨트롤 클래스: `Wft<Component>` (예: `WftActionButton`, `WftPopover`).
- 스타일 키: `Style.<Component>.<Variant>` (예: `Style.ActionButton.Active`).
- 테마 파일: `Themes/Dark.xaml`, `Themes/Light.xaml`, `Themes/HighContrast.xaml`, 공통 `Themes/Tokens.xaml`.
- 디자인 시안 파일(Figma 등)의 컴포넌트 이름은 위 클래스명과 동일하게 유지한다.

---

## 12. 디자인 QA 체크리스트
- [ ] 모든 색상이 토큰으로만 참조되며 하드코딩 HEX가 XAML에 없다.
- [ ] 버튼 44/56/72 세 크기에서 아이콘·라벨이 잘리지 않는다.
- [ ] 라벨 숨김 상태에서도 스크린리더 이름이 존재한다.
- [ ] 다크/라이트/고대비 3테마에서 §2.1 대비 기준을 만족한다.
- [ ] 투명도 20%에서 히트테스트가 유지된다(`Color.HitTest` 적용).
- [ ] 팝업 패널이 화면 밖으로 나가지 않으며 바 반대편으로 열린다.
- [ ] 컨텍스트 버튼 등장/소멸 시 사용자 기능 버튼 위치가 변하지 않는다.
- [ ] 그림자, 그라디언트, 지속 애니메이션이 사용되지 않았다.
- [ ] 모든 문구가 §9 용어 통일표를 따른다.
- [ ] 200% DPI에서 1px 테두리가 흐릿하지 않다(`UseLayoutRounding=True`, `SnapsToDevicePixels=True`).
- [ ] 온보딩 3단계가 터치만으로 60초 내 완료된다.
